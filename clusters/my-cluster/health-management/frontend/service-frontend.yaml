apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: health-management
  labels:
    app.kubernetes.io/name: frontend
spec:
  ports:
  - name: frontend-port
    port: 80
    targetPort: 80
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: frontend